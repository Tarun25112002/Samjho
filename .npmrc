# Fail installs when the lockfile is out of date instead of silently updating it.
# Keeps CI honest about dependency changes.
strict-peer-dependencies=false

# Hoist nothing implicitly — a package must declare what it imports.
# This is the main reason we use pnpm: it makes phantom dependencies impossible.
shamefully-hoist=false

auto-install-peers=true
